name: patternbloom
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pytorch=2.4.0
  - pytorch-cuda=12.4
  - cuda-toolkit=12.4
  - pip:
      - -r requirements.txt
